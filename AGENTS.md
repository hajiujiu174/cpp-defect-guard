# CodeGuard 项目协作指令与改进优先级

更新日期：2026-09-10。适用范围：本文件所在项目及其子目录。

## 每次任务开始时

1. 先读取本文件，再查看当前用户要求、Git 工作区状态和相关模块的实际代码。若上下文中已完整提供本文件，则直接遵循；继续跨轮任务时保留已完成工作，避免重复执行。
2. 涉及项目方向、完成状态或后续开发时，读取 `docs/codeguard-status.md` 和 `docs/level3-acceptance.md`，按需查看相关模块说明。文档与代码不一致时核实并说明，不沿用过时结论。
3. 用户当次明确要求决定任务范围；下面的优先级用于未指定方向时选择后续工作，不自动授权执行整份路线图。回答、评审或列计划时，不擅自修改代码、推送或发布。
4. 使用中文回复。保护用户已有改动，不回退无关文件；实现后完成与变更相称的验证，说明已验证与未验证的部分。

## 项目定位与当前基线

主线是“基于静态程序分析的跨平台 C/C++ 软件质量分析与工程管理系统”，课程交付目标为 Level 3。架构为 C++20 Core、CLI、Qt 前端和 SQLite；Python 模型实验保留为 Level 4 研究资产，不作为核心程序的运行前提。

2026-09-10 用户调整：实际交付只面向 Windows，暂缓跨平台建设与 Linux 全功能验收。保留已有 POSIX 实现和历史测试记录，不将新增平台支持当作当前验收条件；未经用户重新指定，不自动恢复跨平台开发。

截至上述日期，已实现五类 Clang AST 规则、多 TU 线程池、单写线程批量事务、自研查询语言、Windows/POSIX 进程管理、源码副本内 CMake/CTest、Git 信息和构建历史，已接入 CLI/Qt。

历史验收记录：Windows analysis-release 65 项中 64 通过、1 跳过；Windows core-debug 43 项中 42 通过、1 跳过；WSL Linux Core 42 项全部通过。Windows 运行包已完成本机隔离环境变量验收。这些是既有版本的记录，不能直接作为后续改动已通过测试的证据。

当时仍有以下缺口，后续应以实际实现重新核实：

- Windows CI 与锁定依赖的独立构建流程正在本轮补齐；执行结果见 `docs/windows-ci.md`。Linux Clang/Qt 验收已按用户要求暂缓。
- 尚未完成 2—3 个外部真实工程的系统验收及规则准确性评测。
- 文件变化统计已有，AST 结果复用与增量解析尚未实现。
- Qt 查询同步执行，历史加载与大规模结果展示仍需改进。
- 五类规则主要在 Clang 适配器中实现，配置、抑制与独立规则组织仍需完善。
- 部分早期文档仍有“线程池尚未完成”“尚未打包”等过时表述，需与最新验收记录统一。

## 改进优先级

总体顺序：交付可复现 → 真实工程可用 → 分析能力深化 → 研究扩展。同级按下列编号推进；真实工程发现的阻断问题优先解决。

### P0-1：构建与交付可复现

- 建立 Windows GitHub Actions，锁定依赖版本及归档校验值，在独立工具链目录完成构建、测试和打包，再在新的 Windows runner 验证运行包。跨平台工作暂缓。
- 校准 README、路线图及模块说明中的过时状态，将历史验收与当前能力明确区分。
- 完成标准：干净克隆后按说明可构建、测试；失败保存日志；运行包在未还原该工具链的新 Windows runner 上通过检查。CI 机器的自动窗口测试不等于所有实体电脑、DPI 和显卡组合验收。

### P0-2：真实工程验收

- 选择 2—3 个许可明确、不同规模并覆盖 C/C++ 的真实项目，固定版本和配置。
- 记录解析覆盖、失败原因、告警、构建测试、耗时与内存，对告警进行人工复核。
- 完成标准：实验可复现，每个项目有结论及未覆盖范围；构造样例结果不替代真实工程准确性评测。

### P1-1：工程导入与配置管理

- 自动发现编译数据库，缺失时引导生成；持久化编译器、目标、构建参数及规则配置。
- 支持按项目调整副本复制范围，明确处理外部依赖和生成文件。
- 完成标准：新用户能完成导入、分析、构建和测试；重启可恢复配置；失败原因可理解、可定位。

### P1-2：规则引擎模块化与可信度

- 将集中在 Clang 适配器内的检查拆为独立规则，增加启停、级别配置和带理由的抑制。
- 补充宏、模板及误报反例，建立可复现评测集。
- 完成标准：每条规则可独立测试和配置；分别记录命中、误报和已标注范围内的漏报；解析失败不能被表示为无问题。

### P1-3：报告与版本对比

- 在 C++ 主线增加 HTML/JSON 报告，统一展示问题、解析覆盖、复杂度和测试结果。
- 对比新增、消失和持续存在的问题，保留扫描版本及配置关联。
- 完成标准：报告可独立查看并追溯；“问题消失”能区分修复、文件删除和解析失败。

### P2-1：大工程性能与交互

- 先将查询、历史加载改为后台任务并支持取消，再实现按编译单元缓存及头文件依赖失效。
- 缓存有效性应覆盖源码、依赖头文件、编译参数、工具链与分析器/规则版本；不能只根据源文件时间戳复用结果。
- 完成标准：大查询期间界面保持响应；缓存失效正确；增量结果与全量结果一致，性能改进有受控实测支撑。

### P2-2：更深的分析和导航

- 增加引用索引、查找引用、最大嵌套和扇入扇出；逐步加入局部空值传播、数组区间分析。
- 在关系数据可靠后补充交互图、路径查看及源码跳转。
- 完成标准：每项分析有明确边界和正反例；无法判断的复杂路径保留未知状态，不虚构确定结论。

### P3：AI 与自动修复

- 在前述工程主线稳定后，先做少量确定性修复模板及差异预览，再评估模型部署；研究实验限定范围。
- 候选补丁接入现有副本构建测试流程，由用户决定是否应用到原始工程。
- 完成标准：每个候选都有差异及验证记录；分别评价编译通过、测试通过和修复正确，不混为一谈。

## 建议执行轮次

1. 第一轮：Windows CI、依赖锁定、文档校准、独立运行包验收；跨平台暂缓，真实工程测试集属于随后 P0-2。
2. 第二轮：根据真实工程反馈完善导入配置、规则管理、报告导出。
3. 第三轮：先解决界面阻塞，再推进增量分析、引用索引及局部数据流规则。
4. 第四轮：限定范围开展 AI 或自动修复实验。

若用户要求继续改进但没有指定方向，优先选择尚未完成的 P0-1；已完成项以实际代码和验收证据判断。完成开发任务后，同步更新相关项目状态文档；涉及本计划进度变化时更新对应状态并保留必要验收边界，不把计划目标写成已实现能力。
