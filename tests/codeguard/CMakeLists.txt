add_executable(codeguard-core-tests core_tests.cpp)
find_package(GTest CONFIG QUIET)
if(GTest_FOUND)
    add_executable(codeguard-contract-tests contracts_gtest.cpp)
    target_link_libraries(codeguard-contract-tests PRIVATE codeguard-core GTest::gtest_main)
    add_test(NAME codeguard.google_contracts COMMAND codeguard-contract-tests)
endif()
add_executable(codeguard-process-helper process_helper.cpp)
target_link_libraries(codeguard-process-helper PRIVATE codeguard-core)
if(MINGW)
    target_link_options(codeguard-process-helper PRIVATE -municode)
endif()
add_executable(codeguard-level3-tests level3_tests.cpp)
target_link_libraries(codeguard-level3-tests PRIVATE codeguard-core)
foreach(case pool git build_pass build_failure build_testfail build_notests build_timeout build_cancel build_stale)
    add_test(NAME codeguard.${case} COMMAND codeguard-level3-tests ${case})
    set_tests_properties(codeguard.${case} PROPERTIES TIMEOUT 60 LABELS level3)
endforeach()
foreach(case process_args process_flood process_failure process_timeout process_cancel)
    add_test(NAME codeguard.${case} COMMAND codeguard-level3-tests ${case} $<TARGET_FILE:codeguard-process-helper>)
    set_tests_properties(codeguard.${case} PROPERTIES TIMEOUT 15 LABELS level3)
endforeach()
if(CODEGUARD_ENABLE_ANALYSIS)
    foreach(case rules parallel)
        add_test(NAME codeguard.${case} COMMAND codeguard-level3-tests ${case})
        set_tests_properties(codeguard.${case} PROPERTIES TIMEOUT 60 LABELS level3)
    endforeach()
endif()
target_link_libraries(codeguard-core-tests PRIVATE codeguard-core SQLite3::SQLite3)
foreach(case scan empty ignore missing persistence changes rollback readonly foreign_db source_protection unicode symlink)
    add_test(NAME codeguard.${case} COMMAND codeguard-core-tests ${case})
endforeach()
set_tests_properties(codeguard.symlink PROPERTIES SKIP_REGULAR_EXPRESSION "SKIP:")
add_test(NAME codeguard.cli_help COMMAND codeguard-cli --help)
add_executable(codeguard-query-tests query_tests.cpp)
target_link_libraries(codeguard-query-tests PRIVATE codeguard-core)
foreach(case pipeline precedence ordering errors bounds coverage)
    add_test(NAME codeguard.query_${case} COMMAND codeguard-query-tests ${case})
endforeach()
add_test(NAME codeguard.cli_reject COMMAND codeguard-cli unsupported)
set_tests_properties(codeguard.cli_reject PROPERTIES WILL_FAIL TRUE)
add_executable(codeguard-analysis-tests analysis_tests.cpp)
target_link_libraries(codeguard-analysis-tests PRIVATE codeguard-core SQLite3::SQLite3)
find_package(Threads REQUIRED)
add_executable(codeguard-cancellation-tests cancellation_tests.cpp)
target_link_libraries(codeguard-cancellation-tests PRIVATE codeguard-core SQLite3::SQLite3 Threads::Threads)
foreach(case cancel_new cancel_scan cancel_transaction cancel_new_transaction cancel_fence cancel_after_commit)
    add_test(NAME codeguard.${case} COMMAND codeguard-cancellation-tests ${case})
endforeach()
foreach(case graphs migration)
    add_test(NAME codeguard.${case} COMMAND codeguard-analysis-tests ${case})
endforeach()
if(CODEGUARD_ENABLE_ANALYSIS)
    add_test(NAME codeguard.cancel_analysis COMMAND codeguard-cancellation-tests cancel_analysis)
    foreach(case clang partial missing_command ambiguous unsafe identity)
        add_test(NAME codeguard.${case} COMMAND codeguard-analysis-tests ${case})
    endforeach()
    set(RELATIONS_SOURCE "${PROJECT_SOURCE_DIR}/samples/codeguard-relations")
    set(RELATIONS_BUILD "${CMAKE_CURRENT_BINARY_DIR}/relations-build")
    add_test(NAME codeguard.fixture_configure COMMAND "${CMAKE_COMMAND}"
        -S "${RELATIONS_SOURCE}" -B "${RELATIONS_BUILD}" -G Ninja "-DCMAKE_CXX_COMPILER=${CMAKE_CXX_COMPILER}")
    set_tests_properties(codeguard.fixture_configure PROPERTIES FIXTURES_SETUP relations_database)
    add_test(NAME codeguard.analysis_cli COMMAND "${CMAKE_COMMAND}"
        "-DCLI=$<TARGET_FILE:codeguard-cli>" "-DSOURCE=${RELATIONS_SOURCE}"
        "-DDATABASE=${CMAKE_CURRENT_BINARY_DIR}/cli-analysis.sqlite3" "-DCOMMANDS=${RELATIONS_BUILD}"
        -P "${CMAKE_CURRENT_SOURCE_DIR}/cli_analysis.cmake")
    set_tests_properties(codeguard.analysis_cli PROPERTIES FIXTURES_REQUIRED relations_database)
    if(CODEGUARD_BUILD_GUI)
        set(LEVEL3_SOURCE "${PROJECT_SOURCE_DIR}/samples/level3-demo")
        set(LEVEL3_BUILD "${CMAKE_CURRENT_BINARY_DIR}/level3-demo-build")
        add_test(NAME codeguard.level3_configure COMMAND "${CMAKE_COMMAND}" -S "${LEVEL3_SOURCE}" -B "${LEVEL3_BUILD}" -G Ninja "-DCMAKE_CXX_COMPILER=${CMAKE_CXX_COMPILER}")
        set_tests_properties(codeguard.level3_configure PROPERTIES FIXTURES_SETUP level3_commands)
        add_test(NAME codeguard.level3_cli COMMAND "${CMAKE_COMMAND}" "-DCLI=$<TARGET_FILE:codeguard-cli>"
            "-DSOURCE=${LEVEL3_SOURCE}" "-DDATABASE=${CMAKE_CURRENT_BINARY_DIR}/level3-cli.sqlite3"
            "-DCOMMANDS=${LEVEL3_BUILD}" "-DOUTPUT=${CMAKE_CURRENT_BINARY_DIR}/level3-cli-runs"
            -P "${CMAKE_CURRENT_SOURCE_DIR}/level3_cli.cmake")
        set_tests_properties(codeguard.level3_cli PROPERTIES FIXTURES_REQUIRED level3_commands TIMEOUT 90)
        file(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/gui-level3")
        add_test(NAME codeguard.level3_gui COMMAND codeguard-gui --engineering-smoke "${LEVEL3_SOURCE}"
            --database "${CMAKE_CURRENT_BINARY_DIR}/gui-level3/scan.sqlite3" --compile-commands "${LEVEL3_BUILD}")
        set_tests_properties(codeguard.level3_gui PROPERTIES FIXTURES_REQUIRED level3_commands
            ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 60 PASS_REGULAR_EXPRESSION "GUI_LEVEL3_OK")
        file(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/gui-analysis")
        add_test(NAME codeguard.analysis_gui COMMAND codeguard-gui --smoke-test "${RELATIONS_SOURCE}"
            --database "${CMAKE_CURRENT_BINARY_DIR}/gui-analysis/inventory.sqlite3" --compile-commands "${RELATIONS_BUILD}")
        set_tests_properties(codeguard.analysis_gui PROPERTIES FIXTURES_REQUIRED relations_database
            ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 30 PASS_REGULAR_EXPRESSION "GUI_SMOKE_OK")
    endif()
endif()
if(CODEGUARD_BUILD_GUI)
    find_package(Qt6 6.2 REQUIRED COMPONENTS Widgets)
    add_executable(codeguard-qt-scale-tests qt_scale_tests.cpp)
    target_link_libraries(codeguard-qt-scale-tests PRIVATE codeguard-qt-task Qt6::Widgets SQLite3::SQLite3)
    add_test(NAME codeguard.qt_lock_cancel COMMAND codeguard-qt-scale-tests lock)
    add_test(NAME codeguard.qt_read_lock_cancel COMMAND codeguard-qt-scale-tests read_lock)
    add_test(NAME codeguard.qt_scale_inventory COMMAND codeguard-qt-scale-tests inventory 2048)
    set_tests_properties(codeguard.qt_lock_cancel codeguard.qt_read_lock_cancel codeguard.qt_scale_inventory PROPERTIES
        ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 60 LABELS "scale")
    if(CODEGUARD_ENABLE_ANALYSIS)
        add_test(NAME codeguard.qt_scale_clang COMMAND codeguard-qt-scale-tests clang 128)
        set_tests_properties(codeguard.qt_scale_clang PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 150 LABELS "scale")
        add_test(NAME codeguard.qt_scale_window COMMAND codeguard-qt-scale-tests window 128 $<TARGET_FILE:codeguard-gui>)
        set_tests_properties(codeguard.qt_scale_window PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 150 LABELS "scale")
    endif()
    add_executable(codeguard-qt-task-tests qt_task_tests.cpp)
    target_link_libraries(codeguard-qt-task-tests PRIVATE codeguard-qt-task Qt6::Widgets)
    add_test(NAME codeguard.qt_background COMMAND codeguard-qt-task-tests)
    set_tests_properties(codeguard.qt_background PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 20)
    add_test(NAME codeguard.gui_smoke COMMAND codeguard-gui
        --smoke-test "${PROJECT_SOURCE_DIR}/samples/fixed"
        --database "${CMAKE_CURRENT_BINARY_DIR}/gui-smoke.sqlite3")
    set_tests_properties(codeguard.gui_smoke PROPERTIES
        ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 20
        PASS_REGULAR_EXPRESSION "GUI_SMOKE_OK")
endif()
